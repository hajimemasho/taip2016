package queriesModule.java;
/**
 * 
 * @author Carmen Cojocaru
 * Class that manages the user needs. 
 * - class used in Facade pattern
 */
public class Query {
	
	QueryType queryType;
	
	/**
	 * Method that processes the input from the Interface module.
	 * @param input
	 * @return
	 */
	QueryType processInput(String input) {
		return queryType;
	}
}
